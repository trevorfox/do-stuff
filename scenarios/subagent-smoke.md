Run a delegation smoke test in this repository.

Requirements:
1. Explicitly invoke the `/orchestration-tester` subagent.
2. Have that subagent validate delegation behavior and return its evidence markers.
3. Keep the final answer short and include whether the delegation test passed or failed.

Do not skip subagent invocation.
